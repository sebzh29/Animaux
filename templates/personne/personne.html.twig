{% extends 'base.html.twig' %}

{% block title %}Personnes{% endblock %}

{% block monTitre %}{{personne.nom}}{% endblock %}
{% block body %}

{# <h3>Liste des animaux possédés : </h3>
{% for possede in personne.possedes %}
    <div class="row allign-items-center">
        <div class="col-2">
            {# <img src="{{asset('images/' ~ possede.animal.image)}}"> #}
        {# </div>
        <div class="col">
            <h2><a href="{{path('afficher_animal',{'id':possede.animal.id})}}">{{possede.animal.nom}}</a></h2>
            <div>Nombre : {{possede.nombre}}</div>
        </div>
    </div> #}
{# {% endfor %} #} 

{% endblock %}
